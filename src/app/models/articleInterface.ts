export interface Article {
    name: string,
    brand: string,
    price: number,
    url: string | null,
    is_affiliate: boolean,
    img: string,
    description: string,
    keywords: string,
    category: string
}
